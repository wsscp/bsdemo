package cc.oit.bsmes.wip.dao;

import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Map;

import javax.annotation.Resource;

import org.junit.Test;

import cc.oit.bsmes.junit.BaseTest;

public class WorkOrderDAOTest extends BaseTest {

	@Resource
	private WorkOrderDAO workOrderDAO;

	@Test
	public void test() {
		List<Map<String, Object>> map = new ArrayList<Map<String, Object>>();
		// workOrderDAO.("20150519175744427");
		for (Map<String, Object> stringObjectMap : map) {

			Iterator<String> it = stringObjectMap.keySet().iterator();
			while (it.hasNext()) {
				String key = it.next();
				System.out.println(key + ":" + stringObjectMap.get(key));
			}
		}
	}

}
