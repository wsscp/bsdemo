package cc.oit.bsmes.pla.dao;

import javax.annotation.Resource;

import org.junit.Test;

import cc.oit.bsmes.junit.BaseTest;
import cc.oit.bsmes.pla.model.CustomerOrderItem;

import java.util.List;

public class CustomerOrderItemDAOTest extends BaseTest{
	
	@Resource
	private CustomerOrderItemDAO customerOrderItemDAO;
	
	@Resource
	private CustomerOrderItemProDecDAO customerOrderItemProDecDAO;
	
	@Resource
	private CustomerOrderItemDecDAO customerOrderItemProDAO;

	@Test
	public void test() {
/*		List<ProductOrderStatus> list = new ArrayList<ProductOrderStatus>();
		list.add(ProductOrderStatus.TO_DO);
		list.add(ProductOrderStatus.FINISHED);
		List<CustomerOrderItem> coiList = customerOrderItemDAO.getByStatus("03",list);
		logger.info(coiList.size());*/
		
/*		CustomerOrderItemDec coid = customerOrderItemProDAO.getById("a251ef2f-7e57-46c1-8d8a-eb6b7acca790");
		logger.info(coid.getLength());
		logger.info(coid.getCusOrderItemProDesList().size());*/
		
/*		List<CustomerOrderItemProDec> list = customerOrderItemProDecDAO.getByCusOrderItemDecId("a251ef2f-7e57-46c1-8d8a-eb6b7acca790");
		logger.info(list.size());*/
		
/*		CustomerOrderItem coi = new CustomerOrderItem();
		List<CustomerOrderItem> list =  customerOrderItemDAO.find(coi);
		logger.info(list.get(0).getCusOrderItemDesc().size());
		logger.info(list.get(0).getCustomerOrder().getId());
		logger.info(list.size());*/
/*		CustomerOrderItemInfo info = new CustomerOrderItemInfo();
		List<CustomerOrderItemInfo> list = customerOrderItemDAO.findByOrderInfo(info);
		logger.info(list.size());*/
		
/*		CustomerOrderItem item = customerOrderItemDAO.getById("4b618453-1b2f-4f08-85b9-40c587f4a055");
		System.out.println(item.getCustomerOrder());
		System.out.println(item.getCusOrderItemDesc());*/


//        List<CustomerOrderItem> list = customerOrderItemDAO.findByContractNoOrProductType(new CustomerOrderItem());
//
//        for(CustomerOrderItem item : list){
//            logger.info(item.getCustomerOrderId());
//            logger.info(item.getId());
//        }
//        assertEquals(8,list.size());
//
//        int size = customerOrderItemDAO.countByContractNoOrProductType(new CustomerOrderItem());
//
//        assertEquals(8,size);
	}

}
