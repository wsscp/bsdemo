Ext.define("bsmes.view.MaterialCheckPanel", {
		extend : 'Ext.panel.Panel',
		autoScroll : true,
		alias : 'widget.materialCheckPanel',
		itemId : 'materialCheckPanel',
		width : document.body.scrollWidth,
		height : document.body.scrollHeight,
		modal : true,
		plain : true,
		items : [{
			xtype: 'materialCheckGrid'
		}],
		initComponent : function() {
			var me = this;
			
			this.callParent(arguments);
		}
});