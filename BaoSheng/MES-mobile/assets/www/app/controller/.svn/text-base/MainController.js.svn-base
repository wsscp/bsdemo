Ext.define('MES.controller.MainController', {
	extend : 'Ext.app.Controller',
	requires : ['Ext.util.Geolocation',
			'Ext.dataview.List', 'Ext.ux.WeiNaiList'],
	config : {
		refs : {
			mainView : '#mainView'
		},
		control : {
			mainView : {
				activeitemchange : 'onActiveItemChange'
			}
		}
	},

	onActiveItemChange : function(view, value, oldValue, eOpts ) {
        switch(value.id) {
        	case "weinaiView":
        		
        		break;
        	default : 
        		//Ext.Msg.alert('Title', '功能暂未开发.', Ext.emptyFn);
        }
	}
});